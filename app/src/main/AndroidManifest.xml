<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barterapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".application.AppClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ForgotPassword"></activity>
        <activity android:name=".activities.TradesHistory" />
        <activity android:name=".activities.OfferAcceptReject" />
        <activity android:name=".activities.CustomOffer" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".activities.Wallet" />
        <activity android:name=".activities.BuySellHistoryServicesDetails" />
        <activity android:name=".activities.History" />
        <activity android:name=".activities.ChangePassword" />
        <activity android:name=".activities.TermsAndConditions" />
        <activity android:name=".activities.TaskDueDate" />
        <activity android:name=".activities.AddTask" />
        <activity android:name=".activities.NotificationsSettings" />
        <activity android:name=".activities.Portfolio" />
        <activity android:name=".activities.MakeOffer" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.MainPage" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.SignInActivity" />
        <activity android:name=".activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>